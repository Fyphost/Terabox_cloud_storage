# In dev, this lets the Next dev server proxy /api/v1/* to the local backend.
# In production with Nginx, this can stay unset — Nginx routes /api/v1/* directly.
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
